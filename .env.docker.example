# Copy to .env.docker. Never commit the populated file.
JWT_SECRET=generate-at-least-32-random-characters
AUDIT_LOG_HMAC_KEY=generate-a-different-32-character-random-secret
FIELD_ENCRYPTION_KEY_VERSION=v1
FIELD_ENCRYPTION_KEY=base64-encoded-32-byte-key
FIELD_ENCRYPTION_KEY_PREVIOUS=
CAPTCHA_SECRET=provider-secret
KHALTI_SECRET_KEY=khalti-live-secret
KHALTI_RETURN_URL=http://localhost:3000/payment/success
KHALTI_WEBSITE_URL=http://localhost:3000
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
SECURITY_ALERT_EMAIL=
AUDIT_RETENTION_DAYS=180
PASSWORD_EXPIRY_DAYS=90

# Compose interpolation variables are read from a separate local .env file:
# MONGO_ROOT_USERNAME=everblue
# MONGO_ROOT_PASSWORD=use-a-long-url-safe-random-password
